# ESP-IDF build output
build/
managed_components/

# dependencies.lock is INTENTIONALLY committed — it pins exact component
# versions and lets the Component Manager skip its if-clause evaluator (which
# breaks on a fresh CI build before component Kconfigs are registered).

# Per-user configuration (contains WiFi creds, etc.)
sdkconfig
sdkconfig.old

# Local debug / capture files
*.log
live.log

# Binaries downloaded for ad-hoc OTA pushes
ota-test/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
